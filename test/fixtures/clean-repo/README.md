# Example repository for CI — no real secrets.

This fixture should pass `offsend check` with policy checks enabled.

Add AI ignore files in your own projects; this minimal repo is only for action smoke tests.
